[Unit]
Description=dtun dual transport authenticated tunnel daemon
After=network.target

[Service]
Type=simple
ExecStart=/usr/bin/dtund -c /etc/dtun/dtun.conf
Restart=on-failure
RestartSec=5s

[Install]
WantedBy=multi-user.target
